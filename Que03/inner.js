const userDetails = {
    name:"Raghuvanshi",
    lastName: 'Tyaagi',
    ageInYear: 2000,
}

function ageInDays(userInfo){
  const fullName = `My full name is ${userInfo.name} ${userInfo.lastName}`;
  const calculateAgeInDays = 
  console.log(fullName)
}
ageInDays(userDetails)